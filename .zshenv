# Zsh Environment Variables
# This file is sourced for all zsh invocations (including non-interactive)
# For interactive shell configuration, see .zshrc

# Source environment variables from .config/zsh/env.zsh
[ -f "$HOME/.config/zsh/env.zsh" ] && source "$HOME/.config/zsh/env.zsh"
