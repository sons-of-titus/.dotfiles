animations {
    enabled = true

    bezier = smoothOut, 0.05, 0.9, 0.1, 1
    bezier = smoothIn, 0.15, 0.85, 0.3, 1
    bezier = bounce, 0.05, 1.2, 0.5, 1
    bezier = overshot, 0.05, 0.8, 0.2, 1

    animation = layersIn, 1, 4, smoothOut, slide
    animation = layersOut, 1, 3, smoothIn, slide
    animation = fadeLayers, 1, 3, smoothIn

    animation = windowsIn, 1, 4, smoothOut
    animation = windowsOut, 1, 3, smoothIn
    animation = windowsMove, 1, 5, smoothOut
    animation = workspaces, 1, 4, overshot

    animation = specialWorkspace, 1, 3, bounce, slidefadevert, 10%

    animation = fade, 1, 4, smoothIn
    animation = fadeDim, 1, 4, smoothIn
    animation = border, 1, 5, smoothIn
}
